name: music_app
description: Personal music library client
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  path_provider: ^2.1.5
  path: ^1.9.0
  file_picker: ^8.3.7
  just_audio: ^0.10.4
  just_audio_windows: ^0.2.2
  audio_service: ^0.18.16
  crypto: ^3.0.6
  image: ^4.5.4
  audio_metadata_reader: ^1.2.0
  desktop_drop: ^0.8.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
