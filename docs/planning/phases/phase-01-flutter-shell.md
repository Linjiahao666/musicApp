# phase-01 Flutter 双端空壳能启动

**What to build:** Android 与 Windows 能启动应用。LibrarySession 空壳就位，三个端口可替换。

## 验收

- [x] Flutter 工程可在 Android 与 Windows 目标编译（本阶段不要实际跑 test/analyze）
- [x] LibrarySession 存在，依赖 StoreFiles、LocalDisk、AudioEngine 接口
- [x] 提供 FakeStoreFiles、FakeLocalDisk、FakeAudioEngine
- [x] 至少一条测试文件描述空曲库投影（写好测试即可，不要执行）
